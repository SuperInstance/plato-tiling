[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tiling"
version = "0.1.0"
description = "Core tile creation, ghost lifecycle, adaptive search, health decay"
license = {text = "MIT"}
requires-python = ">=3.8"
